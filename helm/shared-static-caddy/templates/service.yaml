apiVersion: v1
kind: Service
metadata:
  name: {{ include "shared-static-caddy.fullname" . }}
  labels:
    {{- include "shared-static-caddy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "shared-static-caddy.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http

