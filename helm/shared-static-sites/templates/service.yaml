apiVersion: v1
kind: Service
metadata:
  name: {{ include "shared-static-sites.fullname" . }}
  labels:
    {{- include "shared-static-sites.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "shared-static-sites.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http

