services:
  web:
    image: nginx:1.27-alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d/sites.conf:/etc/nginx/conf.d/sites.conf:ro
      - ./content/sites:/srv/sites:ro
